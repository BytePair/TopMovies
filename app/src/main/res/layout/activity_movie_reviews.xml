<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/movie_reviews_fl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.MovieReviewsActivity">

    <include
        android:id="@+id/movie_reviews_error"
        layout="@layout/full_screen_error" />

    <ProgressBar
        android:id="@+id/movie_reviews_pb"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/movie_reviews_rv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/vertical_padding_small"
        android:paddingTop="@dimen/vertical_padding_small" />

</FrameLayout>